[package]
name = "kamiya_database"
description = "Library for interaction with Kamiya database."
readme = "../README.md"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.160", features = ["derive"] }
